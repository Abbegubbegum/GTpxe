[Unit]
Description=Restart dnsmasq when network interface comes up
After=network.target

[Service]
Type=oneshot
# Wait a moment for the interface to be fully ready
ExecStartPre=/bin/sleep 2
ExecStart=/bin/systemctl restart dnsmasq.service

[Install]
WantedBy=multi-user.target
